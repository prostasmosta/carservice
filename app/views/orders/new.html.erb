<h1 class="mt-3"><%= t('views.new_order') %></h1>

<div class="row">
  <div class="col-md-8">
    <%= render 'form', order: @order, locals: { service_categories: @service_categories } %>
  </div>
</div>
